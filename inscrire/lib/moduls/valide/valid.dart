import 'package:flutter/material.dart';

class Valid extends StatelessWidget {
  const Valid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text(
          'Inscrire successfully'
        ),
      ),
    );
  }
}
